apiVersion: v1
kind: Service
metadata:
  name: {{ include "ark-apiserver.fullname" . }}
  labels:
    {{- include "ark-apiserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "ark-apiserver.selectorLabels" . | nindent 4 }}
