apiVersion: ark.mckinsey.com/v1alpha1
kind: Agent
metadata:
  name: test-agent
  namespace: default
spec:
  prompt: "You are a helpful assistant that remembers names. When someone tells you their name, remember it for the entire conversation."
  modelRef:
    name: default

