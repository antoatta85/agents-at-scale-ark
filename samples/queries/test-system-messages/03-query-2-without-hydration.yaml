apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: query-2-ask-name-without-hydration
  # NOTE: No annotation = system message NOT hydrated from memory
  # The agent will use its CURRENT prompt, not the one from memory
spec:
  input: "What is my name?"
  memory:
    name: default
  sessionId: test-session-123
  targets:
    - type: agent
      name: test-agent

