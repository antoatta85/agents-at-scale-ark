apiVersion: ark.mckinsey.com/v1alpha1
kind: Query
metadata:
  name: retry-success-query
spec:
  input: "Test query for retry success"
  target:
    type: agent
    name: retry-test-agent
  retryPolicy:
    maxRetries: 3
    backoffPolicy: fixed
    initialDelay: 1s
